@using App.Domain.Products.Dtos
@model Muslim.HandelResult.Result<CreateProductDto>
@{
    var data = Model.Response;
    var Page = Model.Page;
}
<link href="~/pagescss/css/public.css" rel="stylesheet" />
<link href="~/pagescss/css/popup.css" rel="stylesheet" />

<form asp-action="Create" method="post">
    <!-- Begin Page Content -->
    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="d-sm-flex align-items-center justify-content-between mb-4">

            <h1 class="h3 mb-0 text-gray-800">
                <i class="fa-regular fa-rectangle-list" style="color: #e94e24"></i>
                <span data-english="Add Product" data-arabic="اضافة منتج"></span>
            </h1>
            <div>
                <a href="#">
                    <i id="infoBtn" class="fa fa-info-circle" style="color:#502e90;"></i>
                </a>
                <div id="infoPopup">On this page, the user adds new additional services </div>
            </div>
        </div>
        
        <div class="row" style="margin-left: 20px; margin: 10px;  max-width: 60%;">
            <label style="font-weight: bold; color: black; margin-top:0px; margin-left: 25px;"
                   data-english="Product Name"
                   data-arabic="إسم منتج"></label>

            <input class="form-control form-control-lg"
                   required
                   asp-for="@data.Name"
                   name="Response.Name"
                   type="text"
                   placeholder-ar="إسم منتج"
                   placeholder-en="Product Name"
                   aria-label="Name"
                   style="border-radius: 17px; border: none;"
                   propertyName="Name"
                   validation="@(Json.Serialize(Model.ValidatorError).ToString())">

            

        </div>

        <div class="row" style="margin-left: 20px; margin: 10px;  max-width: 60%;">
            <label style="font-weight: bold; color: black; margin-top:0px; margin-left: 25px;"
                   data-arabic="رقم الموديل"
                   data-english="Model Number"></label>

            <input class="form-control form-control-lg"
                   asp-for="@data.Model"
                   name="Response.Model"
                   type="text"
                   placeholder-ar="رقم الموديل"
                   placeholder-en="Model Number"
                   aria-label="Model"
                   style="border-radius: 17px; border: none;"
                   propertyName="Model"
                   validation="@(Json.Serialize(Model.ValidatorError).ToString())">

        </div>

        <div class="row" style="margin-left: 20px; margin: 10px;  max-width: 60%;">
            <label style="font-weight: bold; color: black; margin-top:0px; margin-left: 25px;"
                   data-english="Barcode Number"
                   data-arabic="الباركود"></label>

            <input class="form-control form-control-lg"
                   asp-for="@data.Barcode"
                   name="Response.Barcode"
                   type="text"
                   placeholder-ar="الباركود"
                   placeholder-en="Barcode Number"
                   aria-label="Barcode"
                   style="border-radius: 17px; border: none;"
                   propertyName="Barcode"
                   validation="@(Json.Serialize(Model.ValidatorError).ToString())">

        </div>

        <div class="row" style="margin-left: 20px; margin: 10px;  max-width: 60%;">
            <label style="font-weight: bold; color: black; margin-top:0px; margin-left: 25px;"
                   data-arabic="الوصف"
                   data-english="Description "></label>

            <textarea required class="form-control form-control-lg"
                      asp-for="@data.Description"
                      name="Response.Description"
                      placeholder-ar="الوصف"
                      placeholder-en="Description"
                      aria-label="Description"
                      style="border-radius: 17px; border: none;"
                      propertyName="Description"
                      validation="@(Json.Serialize(Model.ValidatorError).ToString())"></textarea>

        </div>

        @* add img  *@


        <div class="row" style="margin-left: 20px; margin: 10px;  max-width: 60%;">
            <label style="font-weight: bold; color: black; margin-top:0px; margin-left: 25px;"
                   data-english="Image"
                   data-arabic="صورة"></label>
           
            <input type="file" id="imageInput" name="imageInput" accept="image/*" style="display: none;" onchange="handleFileSelect(event)" />
            <input type="hidden" id="imageData" name="Response.Image" />

            <div class="input-group mb-3">
                <input 
                    disabled 
                    type="text" 
                    class="form-control" 
                    id="file-name" 
                    placeholder-ar="اختر صورة"
                    placeholder-en="Choose Image"
                    aria-label="Choose file" 
                    aria-describedby="button-addon2" 
                    style="border-radius: 17px; border: none;">
                <div class="input-group-append">
                    <button class="btn btn-outline-secondary" type="button" id="button-addon2" onclick="document.getElementById('imageInput').click();" style="border-radius: 17px; border: none; background-color: #502e90; color: white;">Choose</button>
                </div>
            </div>


        </div>


    </div>

    <div class="container" style="margin-top:70px">
        <div class="row">
            <div class="col-8"></div>
            <div class="col-4" style=" justify-content: end; text-align: end;">
                <button type="submit" class="btn  btn-lg new" 
                        style=" color: white; border-radius:20px ;border:2px solid #502e90; padding-left: 35px ; padding-right: 35px; margin-top: -110px;">
                    <span data-english="Add Product" data-arabic="اضافة منتج">
                      
                    </span>
                </button>

            </div>
        </div>
    </div>
</form>

<script src="~/js/validation.js"></script>  
<script src="~/pagesjs/publicjs/popup.js"></script>

<script>
   function handleFileSelect(event) {
       var input = event.target;
       var file = input.files[0];
       document.getElementById("file-name").value = file.name;

       if (file) {
           var reader = new FileReader();
           reader.onload = function (e) {
               var imageData = e.target.result;
               document.getElementById("imageData").value = imageData;
           };
           reader.readAsDataURL(file);
       }
   }
</script>